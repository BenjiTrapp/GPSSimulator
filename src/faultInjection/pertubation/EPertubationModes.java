package faultInjection.pertubation;

/**
 * Perturbation modes, use them in combination with the Perturbation Factory
 * 
 * @author Benjamin Trapp
 */
public enum EPertubationModes
{
	FLIP_BITS,OFF_BY_ONE,ALL_BITS_HIGH,ALL_BITS_LOW,RANDOM_ASCII,TOGGLE_BITS, STUCK_AT, DASH
}
